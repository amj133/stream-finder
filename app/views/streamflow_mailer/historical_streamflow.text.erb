Historical Streamflow for Station <%= @site_id %> from <%= @time.first %> to <%= @time.last %>

Datetime(YYYY-MM-DDTHH+MM:SS+UTC-OFFSET),Discharge(cfs)\r\n
<% @time.each_with_index do |time, index| %>
  <%= "#{time.to_s},#{@discharge[index]}\r\n"%>
<% end %>
